# Step 1 - Setup Gitleaks workflow on main

Copy these files into your GitHub repository root:

```text
.gitleaks.toml
.github/workflows/gitleaks-poc.yml
```

Commit and merge these files into `main` first.

Do not add the fake secret file in this step.
